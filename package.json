{
  "private": true,
  "scripts": {
    "start": "npx run-p watch serve",
    "build": "npx tsc",
    "watch": "npx tsc --watch",
    "serve": "npx http-server -s"
  },
  "devDependencies": {
    "http-server": "^0.12.1",
    "typescript": "^3.7.5",
    "npm-run-all": "^4.1.5"
  }
}
